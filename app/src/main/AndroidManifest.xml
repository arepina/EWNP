<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anast.ewnp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Info" />
        <activity android:name=".ModeChoosing" />
        <activity android:name=".ThemeChoosing" />
        <activity android:name=".MemActivity" />
        <activity android:name=".CheckActivity" />
        <activity android:name=".ThemeChoosingCheckActivity" />
        <activity android:name=".SportActivity" />
        <activity android:name=".EnvActivity" />
        <activity android:name=".IMActivity" />
        <activity android:name=".NewWordToDictionary"></activity>
    </application>

</manifest>
